import React, { useState } from "react";

const Part = (props) => {
  // const [block, setBlock] = useState(props.block);
  // const handleClick = () => (props.block = "Hit");
  return <div className="part"></div>;
};

export { Part };
